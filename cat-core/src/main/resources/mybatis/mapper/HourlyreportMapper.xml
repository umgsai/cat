<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dianping.cat.core.mybatis.hourlyreport.dao.HourlyreportMapper">
    <resultMap id="BaseResultMap" type="com.dianping.cat.core.mybatis.hourlyreport.dao.data.HourlyreportDO">
        <result column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="name" property="name"/>
        <result column="ip" property="ip"/>
        <result column="domain" property="domain"/>
        <result column="period" property="period"/>
        <result column="creation_date" property="creationDate"/>
    </resultMap>

    <sql id="Base_Column_List">
        id ,
        type ,
        name ,
        ip ,
        domain ,
        period ,
        creation_date
    </sql>

    <sql id="setField">
        <set>
            <if test="id != null">id = #{id} ,</if>
            <if test="type != null">type = #{type} ,</if>
            <if test="name != null">name = #{name} ,</if>
            <if test="ip != null">ip = #{ip} ,</if>
            <if test="domain != null">domain = #{domain} ,</if>
            <if test="period != null">period = #{period} ,</if>
            <if test="creationDate != null">creation_date = #{creationDate}</if>
        </set>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyColumn="id"
            parameterType="com.dianping.cat.core.mybatis.hourlyreport.dao.data.HourlyreportDO" keyProperty="id">
        INSERT INTO hourlyreport
        <include refid="setField"/>
    </insert>

    <select id="findByPrimaryKey" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from hourlyreport where id = #{id}
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from hourlyreport
    </select>

    <delete id="deleteByPrimaryKey">
        delete from hourlyreport where id = #{id}
    </delete>

    <update id="updateByPrimaryKey" parameterType="com.dianping.cat.core.mybatis.hourlyreport.dao.data.HourlyreportDO">
        UPDATE hourlyreport
        <include refid="setField"/>
        WHERE id = #{id}
    </update>

    <select id="findAllByDomainNamePeriod" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from hourlyreport
        where period = #{record.period}
        and domain = #{record.domain}
        and name = #{record.name}
        and binary(domain) = binary(#{record.domain})
    </select>

    <select id="findAllByPeriodName" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from hourlyreport
        where period = #{record.period}
        and name = #{record.name}
    </select>

</mapper>